# TODO Phase 0: Shared FastAPI dependencies
# - get_db: yields AsyncSession
# - get_current_user: validates JWT, checks token_version
# - require_password_changed: raises HTTP 403 MUST_CHANGE_PASSWORD if must_change_password is True
