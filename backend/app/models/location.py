# TODO Phase 0: ORM model — CurrentLocation
# Composite PRIMARY KEY: (user_id, group_id)
# Fields: user_id, group_id, latitude, longitude, accuracy (nullable),
#         recorded_at, updated_at, sharing_enabled
