# TODO Phase 0: Admin bootstrap CLI
# Usage: python manage.py create-admin --email <email> --password <password>
# Must be idempotent (re-running does not create a duplicate admin).
# Document as the mandatory first step in README.
